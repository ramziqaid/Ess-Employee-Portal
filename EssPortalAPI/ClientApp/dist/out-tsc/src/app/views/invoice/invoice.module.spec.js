import { InvoiceModule } from './invoice.module';
describe('InvoiceModule', () => {
    let invoiceModule;
    beforeEach(() => {
        invoiceModule = new InvoiceModule();
    });
    it('should create an instance', () => {
        expect(invoiceModule).toBeTruthy();
    });
});
//# sourceMappingURL=invoice.module.spec.js.map